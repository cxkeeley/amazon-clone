⭐ create react app
✅ npx create-react-app .

-------------------------------------------------------
⭐ go to firebase.com
✅ create database
💥 npm install firebase

💥 Then, initialize Firebase and begin using the SDKs for the products you’d like to use.

// Import the functions you need from the SDKs you need
import { initializeApp } from "firebase/app";
import { getAnalytics } from "firebase/analytics";
// TODO: Add SDKs for Firebase products that you want to use
// https://firebase.google.com/docs/web/setup#available-libraries

// Your web app's Firebase configuration
// For Firebase JS SDK v7.20.0 and later, measurementId is optional
const firebaseConfig = {
  apiKey: "AIzaSyAwo5yhKoi3IRTL1X_1c1RwFzL8itme20w",
  authDomain: "clone-69212.firebaseapp.com",
  projectId: "clone-69212",
  storageBucket: "clone-69212.appspot.com",
  messagingSenderId: "804876906822",
  appId: "1:804876906822:web:d380fbfdcf03b5c40b7547",
  measurementId: "G-6MN7F1GB63"
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);
const analytics = getAnalytics(app);

✅ install firebase CLI
💥 npm install -g firebase-tools

Deploy to Firebase Hosting
You can deploy now or later. To deploy now, open a terminal window, then navigate to or create a root directory for your web app.

Sign in to Google
firebase login
Initiate your project
Run this command from your app’s root directory:

firebase init
When you’re ready, deploy your web app
Put your static files (e.g., HTML, CSS, JS) in your app’s deploy directory (the default is “public”). Then, run this command from your app’s root directory:

firebase deploy
After deploying, view your app at clone-69212.web.app

Need help? Check out the Hosting docs

--------------------------------------------------------
⭐ inside src folder make new file firebase.js
and paste all the firebase SDK snippet
💥 you can find the SDK snippet in firebase -> project setting

--------------------------------------------------------
⭐ start building the project
delete this file as you dont need
💥 App.test.js
💥 logo.svg
💥 setupTests.js

⭐ inside the App.js
✅ delete all the inside <div></div>
✅ delete import logo
✅ add import React from 'react';

--------------------------------------------------------
⭐ working with header components
✅ create new file inside src folder with Header.js
✅ rafce
✅ create <img className='header__logo' src="logo">
✅ create <div className='header__search'></div>
✅ create <div className='header__nav'></div>

--------------------------------------------------------
⭐ install material UI for all the icons
✅ npm install @material-ui/core

Fix the upstream dependency conflict, or retry
npm ERR! this command with --force, or --legacy-peer-deps
npm ERR! to accept an incorrect (and potentially broken) dependency resolution.

⭐ install material UI icons
✅ npm install @material-ui/icons
✅ yarn add @material-ui/icons
✅ after that you can start using the icons with import and use
💥 to import it
✅ import SeachIcons from '@material-ui/icons/Search';
💥 to use it
✅ <SeachIcons />

--------------------------------------------------------

⭐ after that working with Header.js and Header.css

--------------------------------------------------------

⭐ Making the Home components
✅ making file Home.js
✅ add import Home from './Home';
✅ render <Home />

💥💥💥 to quickly import klik <Home /> and ctrl+space and choose import

💥 and working with Home.js and Home.css

--------------------------------------------------------

⭐ Making the product dynamically
✅ making file Product.js
✅ add import Product from './Product';
✅ render <Product /> at Home.js
✅ making a callback function at product.JS
✅ const Product = ({ id, title, image, price, rating }) => {}
✅ <p>{title}</p>
✅ <div className='product__rating'>
          {Array(rating)
            .fill()
            .map((_, i) => (
              <p>⭐</p>
            ))}
        </div>

--------------------------------------------------------

⭐ working at the checkout cart
✅ we will need to install react-router-dom
✅ npm install react-router-dom

✅ go to App.js
✅ add import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';
✅ wrap all the apps in <Router> and <Routes>

--------------------------------------------------------

Few Updates :-

1) As per the new updated Firebase we must import Firebase as follows (firebase.js file):
import firebase from 'firebase/compat/app';
import 'firebase/compat/auth';
import 'firebase/compat/firestore';

2) useHistory is updated as useNavigate
import { Link, useNavigate } from 'react-router-dom'
const navigate = useNavigate ();
navigate('/')

3) <Switch> is deprecated in the lastest version of React instead use <Routes>
import {BrowserRouter as Router, Routes, Route} from "react-router-dom";
<Router>
      <div className="app">
        <Routes>
          <Route path = "/" element={[<Header />, <Home />]} />
          <Route path="/checkout" element={<Header />, <Checkout />} />
          <Route path = "/login" element={<Login />} />
        </Routes>
      </div>
    </Router>

4) Use this instead of history.replace
navigate('/orders', { replace: true });

--------------------------------------------------------

⭐ working with checkoutproduct.js
✅ this is a props use in checkout product
const CheckoutProduct = ({id, image, title, price, rating }) => {}

⭐ working with checkout.js

--------------------------------------------------------

⭐✅💥👉